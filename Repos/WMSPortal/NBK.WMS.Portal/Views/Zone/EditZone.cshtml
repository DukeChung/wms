@using NBK.WMS.Portal.Helpers
@model NBK.ECService.WMS.DTO.ZoneDto
@{
    ViewBag.Title = "编辑储区";
}

@Html.Breadcrumb("储区管理", "/Zone/ZoneList", "编辑储区")
<div class="wrapper wrapper-content animated fadeInRight ecommerce">
    <div class="ibox-content  m-b-sm border-bottom">
        <form id="editZoneForm" class="form-horizontal" method="get">
            <input type="hidden" id="SysId" name="SysId" value="@Model.SysId" />
            <input type="hidden" id="CreateBy" name="CreateBy" value="@Model.CreateBy" />
            <input type="hidden" id="CreateDate" name="CreateDate" value="@Model.CreateDate" />
            <input type="hidden" id="CreateUserName" name="CreateUserName" value="@Model.CreateUserName" />
            <div class="form-group">
                <label class="col-sm-2 control-label">储区:</label>
                <div class="col-sm-4"><input type="text" id="ZoneCode" name="ZoneCode" class="form-control" value="@Model.ZoneCode"></div>

                <label class="col-sm-2 control-label">默认拣货到货位:</label>
                <div class="col-sm-4"><input type="text" id="DefaultPickToLoc" name="DefaultPickToLoc" class="form-control" value="@Model.DefaultPickToLoc"></div>
            </div>

            <div class="form-group">
                <label class="col-sm-2 control-label">入库过渡货位:</label>
                <div class="col-sm-4"><input type="text" id="InLoc" name="InLoc" class="form-control" value="@Model.InLoc"></div>

                <label class="col-sm-2 control-label">出库过渡货位:</label>
                <div class="col-sm-4"><input type="text" id="OutLoc" name="OutLoc" class="form-control" value="@Model.OutLoc"></div>
            </div>

            <div class="form-group">
                <label class="col-sm-2 control-label">备注:</label>
                <div class="col-sm-4"><input type="text" id="Descr" name="Descr" class="form-control" value="@Model.Descr"></div>

                <label class="col-sm-2 control-label">启用:</label>
                <div class="col-sm-4">
                    <div class="checkbox i-checks">
                        <input type="checkbox" id="IsActive" name="IsActive" checked="@Model.IsActive"><i></i>
                    </div>
                </div>
            </div>
        </form>
        <div class="ibox-footer text-right tooltip-demo">
            <a class="btn btn-sm btn-primary" id="btnSave" onclick="editZone();"><i class="fa fa-check-square-o"></i> 保存</a>
            <a class="btn btn-white btn-sm" id="btnBack" onclick="goBack();"><i class="fa fa-times"></i> 返回</a>
        </div>
    </div>
</div>

@section Styles {
    @Styles.Render("~/Content/plugins/dataTables/dataTablesStyles")
    @Styles.Render("~/Content/plugins/iCheck/iCheckStyles")
}

@section Scripts {
    @Scripts.Render("~/plugins/dataTables", "~/plugins/validate")
    @Scripts.Render("~/plugins/iCheck")
    @Scripts.Render("~/plugins/validate")
    @Scripts.Render("~/plugins/validate/cnMessage")
    <script src="~/Scripts/Utility/Common.js"></script>
    <script type="text/javascript">
        $(document).ready(function () {
            $('.i-checks').iCheck({
                checkboxClass: 'icheckbox_square-green',
                radioClass: 'iradio_square-green'
            });

            $("#editZoneForm").validate({
                rules: {
                    ZoneCode: {
                        required: true,
                        maxlength: 32
                    },
                    DefaultPickToLoc: {
                        maxlength: 32
                    },
                    InLoc: {
                        maxlength: 32
                    },
                    OutLoc: {
                        maxlength: 32
                    },
                    Descr: {
                        maxlength: 128
                    }
                }
            });
        });

        function editZone() {
            if ($("#editZoneForm").valid()) {
                $.ajax({
                    url: "/Zone/SaveEditZone",
                    type: "POST",
                    data: $("#editZoneForm").serializeObject(),
                    success: function (data) {
                        if (data.Success) {
                            msgSuccess(data.Message);
                            window.location.href = "/Zone/ZoneList";
                        } else {
                            msgErro(data.Message);
                        }
                    },
                    error: function (data) {
                        msgErro(data.Message);
                    }
                });
            }
        }
    </script>
}