@using NBK.WMS.Portal.Helpers
@{
    ViewBag.Title = "承运商管理";
}

@Html.Breadcrumb("基础资料管理", "承运商管理")
<div class="wrapper wrapper-content animated fadeInRight ecommerce">
    <div class="ibox-content m-b-sm border-bottom">
        <div class="row">
            <div class="col-sm-3">
                @Html.SearchTextBox("承运商名称", "CarrierNameSearch")
            </div>
            <div class="col-sm-3">
                @Html.SearchTextBox("承运商联系电话", "CarrierPhoneSearch")
            </div>
            <div class="col-sm-3">
                @Html.SearchTextBox("承运商联系人", "CarrierContactsSearch")
            </div>
            <div class="col-sm-2">
                <label class="control-label">是否启用</label>
                <select id="IsActiveSearch" name="IsActiveSearch" class="form-control">
                    @{
                        foreach (var isActive in (List<SelectListItem>)ViewBag.IsActiveList)
                        {
                            if (Convert.ToBoolean(isActive.Value))
                            {
                                <option value="@isActive.Value" selected>@isActive.Text</option>
                            }
                            else
                            {
                                <option value="@isActive.Value">@isActive.Text</option>
                            }
                        }
                    }
                </select>
            </div>
            <div class="col-sm-1">
                <button id="btnSearch" class="btn btn-primary" type="button" style="margin-top:23px;float: left; ">查询</button>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox">
                <div class="ibox-content" style="overflow-x:auto">
                    <table id="gvCarrier" class="table table-striped table-bordered table-hover dataTables-example">
                        <thead>
                            <tr>
                                <th><input type="checkbox" id="ckbAll" onclick="gvCheckAll(this);" /></th>
                                <th data-data="CarrierName" style="width:100px">承运商名称</th>
                                <th data-data="CarrierPhone" style="width:100px">承运商联系电话</th>
                                <th data-data="OtherCarrierId" style="width:100px">承运商外部ID</th>
                                <th data-data="CarrierContacts" style="width:100px">承运商联系人</th>
                                <th data-data="IsActiveText" style="width:100px">是否启用</th>
                            </tr>
                        </thead>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
@section Styles {
    @Styles.Render("~/Content/plugins/dataTables/dataTablesStyles")
}

@section Scripts {
    @Scripts.Render("~/plugins/dataTables")
    <script src="~/Scripts/Utility/Common.js"></script>
    <script type="text/javascript">
        var table;
        $(document).ready(function () {
            table = $('#gvCarrier').DataTable({
                "sAjaxSource": "GetCarrierList",
                "fnServerParams": function (aoData) { //查询条件
                    aoData.push(
                        { "name": "CarrierNameSearch", "value": $("#CarrierNameSearch").val() },
                        { "name": "CarrierPhoneSearch", "value": $("#CarrierPhoneSearch").val() },
                        { "name": "CarrierContactsSearch", "value": $("#CarrierContactsSearch").val() },
                        { "name": "IsActiveSearch", "value": $("#IsActiveSearch").val() }
                    );
                },
                dom: 'Bfrtip',
                buttons: [
                    {
                        text: '新增',
                        action: function () {
                            window.location.href = "/Carrier/AddCarrier";
                        }
                    },
                    {
                        text: '编辑',
                        action: function (e, dt, node, config) {
                            var sysId = GetGridSysId();
                            if (sysId != null) {
                                window.location.href = "/Carrier/EditCarrier?sysId=" + sysId;
                            }
                        }
                    },
                    {
                        text: '删除',
                        action: function (e, dt, node, config) {
                            var sysIds = GetGridMultiselectSysId();
                            if (sysIds != null) {
                                msgConfirm("确定要删除所选记录吗？", function (isConfirm) {
                                    if (isConfirm) {
                                        $.ajax({
                                            url: "/Carrier/DeleteCarrier?sysIds=" + sysIds,
                                            type: "GET",
                                            success: function (data) {
                                                if (data.Success) {
                                                    msgSuccess(data.Message, setTimeout(function () { table.ajax.reload(); }, 100));
                                                }
                                                else {
                                                    msgErro(data.Message);
                                                }
                                            },
                                            error: function (data) {
                                                msgErro(data.Message);
                                            }
                                        });
                                    }
                                });
                            }
                        }
                    }
                ],
                "columnDefs": [
                    {
                        "targets": 0,
                        "width": "15px",
                        render: function (data, type, full, meta) {
                            return '<input type="checkbox" id="checkbox-all-' + full.SysId + '" value="' + full.SysId + '" />';
                        }
                    }
                ],
                "bServerSide": true,
                "bProcessing": true,
                "bPaginate": true, //翻页功能
                "bLengthChange": true, //改变每页显示数据数量
                "bFilter": false, //过滤功能
                "bSort": false, //排序功能
                "bInfo": true, //页脚信息
                "bAutoWidth": true, //自动宽度
                "aaSorting": [[2, "Asc"]],
                "oLanguage": {
                    "sLengthMenu": "每页显示 _MENU_ 条记录",
                    "sInfo": "从 _START_ 到 _END_ /共 _TOTAL_ 条数据",
                    "sInfoEmpty": "",
                    "sInfoFiltered": "(从 _MAX_ 条数据中检索)",
                    "oPaginate": {
                        "sFirst": "首页",
                        "sPrevious": "前一页",
                        "sNext": "后一页",
                        "sLast": "尾页"
                    },
                    "sZeroRecords": "没有检索到数据"
                }
            });

            $("#btnSearch").bind("click", function () { //按钮 触发table重新请求服务器
                table.ajax.reload();
            });
        });
    </script>
}